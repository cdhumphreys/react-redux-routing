function posts(store = [], action) {
	switch (action.type) {
		case 'ADD_POST':
			// add photo & caption, initialise likes & comment section?
			break;
		default:
			// no action defined
			break;
	}
}

export default posts;